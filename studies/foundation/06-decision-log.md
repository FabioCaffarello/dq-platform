<!-- path: studies/foundation/06-decision-log.md -->

# 06 — Decision Log

## Metadata

- Purpose: track every platform decision that must be resolved before
  implementation. Each row notes priority, current status, the
  rationale for why it matters, and the location of the resolving
  document.
- Audience: project lead, platform engineers, anyone planning the next
  session of work.
- Status: living document. Update whenever a decision changes state.
- Last updated: 2026-05-26 (B2-21 implementation shipped against ADR-0035 §"Migration support level" — `dq-lint` deprecation warning for deprecated schema versions; new `tools/lint/compatibility.go` declares the ADR-0035 compatibility-state table as a Go const map (v1 → deprecated/2026-08-23; v2 → current/TBD) + `CheckDeprecatedSchemaVersions(dir)` walker reusing the existing rule-walker posture (skips `_schema/`, skips `_`-prefixed files, missing-dir is graceful empty); main.go emits warnings to stderr with `DEPRECATED:` prefix before validation errors, no exit-code influence; new `-no-deprecation-warnings` flag suppresses; unit tests cover lookup + walker happy/negative paths + skip semantics + missing-dir behavior; production `rules/` now emits zero warnings since B2-19 migrated customer.yaml to v2)
- Earlier update: 2026-05-26 (B2-19 implementation shipped against ADR-0035 — `rules/customer.yaml` migrated atomically to v2 with `mode: set` + explicit `source: {type: bigquery, project_id: dq-local, dataset_id: dq_fixture, table_id: customer}` matching the demo-substrate seed + kind prefix `set.row_count_positive`; `scripts/smoke/demo-p6.sh` extended with `-supported-schema-versions "1,2"` so the publisher accepts the new shape (engine main.go already declared `{1, 2}`, no engine change); lint + per-module tests + validate-deploy + local dry-run-rules all clean; **unblocks B2-20 v1-retirement engine release** alongside the 90-day floor; pre-existing demo_p6 integration-test vet failure tracked but unrelated to B2-19)
- Earlier update: 2026-05-26 (B2-18 implementation shipped against ADR-0034 — first integration-sandbox tier consumer slice; chose object-store CAS fidelity (ADR-0017 Partial row) as the first capability since fake-gcs-server cannot enforce `ifGenerationMatch` and the publisher's CAS-race-loser contract was only ever exercised against the in-mem fake; new `tools/manifest/publisher_sandbox_test.go` with happy-path + stale-generation-rejection tests guarded by `DQ_SANDBOX_PROJECT` + `DQ_SANDBOX_BUCKET` env vars (skips cleanly without them); three Make targets — `test-tools-manifest-sandbox`, `test-engine-sandbox` (reserved no-op), `test-sandbox` (umbrella); new `.github/workflows/sandbox.yml` CI lane on `workflow_dispatch` only at v1, gated on `vars.SANDBOX_ENABLED` or sandbox-secret presence — explicitly NOT gating the default PR lane per ADR-0034 cost discipline; `docs/dev/local-testing.md` §"integration-sandbox" flipped from "reserved at v1" to documenting the landed test + the three targets + the CI lane)
- Earlier update: 2026-05-26 (B2-17 implementation shipped against ADR-0033 §"Manual trigger semantics" — new operator runbook `docs/runbooks/manual-vs-operator-rerun.md` with the standard six-section anatomy; section 3.A walks a three-question decision tree (replace prior result → operator-rerun; parallel observation → manual; uncovered window → manual); section 3.C explicitly flags the Admin API as v1-unimplemented and documents three concrete workarounds until it lands; section 3.D provides a `dq_executions` read pattern distinguishing retries vs parallel observations vs supersede chains; maturity disclaimer pins the Admin API gap as the load-bearing TBD; README runbook index extended)
- Earlier update: 2026-05-26 (B2-16 implementation shipped against ADR-0033 — first scheduler-consumer slice; (1) optional `schedule` field added to v2 rule schema with `oneOf` gating duration-literal `^[0-9]+(ms|s|m|h|d)$` and 5-field cron `^\\S+ \\S+ \\S+ \\S+ \\S+$` (canonical + mirror byte-equal via `make sync-schema`); (2) `EnvConfig.SchedulerCatchupHorizon` populated 1h/6h/24h for local/qa/prod (advisory; engine doesn't enforce); (3) `Store.LatestExecutionPerEntityCheck(asOf)` reader method + `LatestExecutionRow` type — partition-pruned BQ query joining `dq_executions`+`dq_check_results` and reducing to one row per `(entity, check_id)` keyed on latest `window_end ≤ asOf`; BigQueryStore impl + mockStore + inMemStore + unit test; (4) reference Kubernetes CronJob at `deploy/base/cronjob-scheduler.yaml` — illustrative POST to `/v1/trigger` per ADR-0014, heavy comments on the operator workflow, included in base kustomization; deploy-validate clean (5 resources per overlay); lint fixtures cover both schedule forms + the 4-field cron malformed case)
- Earlier update: 2026-05-26 (B2-15 implementation shipped against ADR-0032 §"Sparse-history policy" — new operator runbook `docs/runbooks/baselined-check-degraded.md` for the `degraded` + `insufficient_baseline_samples` signal that B2-14's first baselined kind can emit; standard six-section anatomy; section 3.B walks four causes ordered by likelihood (new check still warming up; declared `reference_window` exceeds env `ResultsRetention`; `min_samples` set too high for the check's cadence; recent `fail`/`error` rows shrank the `pass` set the baseline query reads) each with a confirmation SQL query + remediation; section 5 explicitly notes degraded is itself the safe state — no destructive rollback; section 6 routes four escalation conditions to platform-team or entity owner; README index extended)
- Earlier update: 2026-05-26 (B2-14 implementation shipped against ADR-0032 — first baselined kind `set.row_count_within_baseline`; new `engine/internal/eval/baselines.go` framework (BaselineSpec/Tolerance/BaselineResult types; closed-grammar ParseReferenceWindow for `[0-9]+(ms|s|m|h|d)` literal; ParseBaselineSpec for both `platform_history` and `static` sub-modes with closed-enum guards; ComputeBaseline issuing a parameterised BQ query against `dq_check_results`+`dq_executions` joined on `(execution_id, attempt_id)`, reading only `pass` rows, capped at `min(declared, ResultsRetention)`; `APPROX_QUANTILES(col, 100)[OFFSET(N)]` idiom for percentiles); new `engine/internal/eval/row_count_within_baseline.go` five-step handler (reuses `rowCountPositiveSQL` so B2-12 partition pruning is inherited; ResultDegraded with `insufficient_baseline_samples` for `samples_used < min_samples`); `EnvConfig.EvidenceRetention.ResultsRetention` wired per-env (30d/90d/365d); catalog v1 extended with full `params_schema` for the baseline block (canonical + mirror); `tools/dryrun` mirror matches the new kind's current-value half (baseline query NOT dry-run-checked); unit tests for helpers + handler dispatch)
- Earlier update: 2026-05-26 (B2-13 implementation shipped against ADR-0031 — new operator runbook `docs/runbooks/results-partition-migration.md` for the one-time `CREATE TABLE ... AS SELECT ... PARTITION BY DATE(recorded_at)` + rename migration; covers both tables (`dq_executions` partitioned by `recorded_at`; `dq_check_results` by `executed_at`); standard six-section anatomy; engine-pause precondition + snapshot-based escape valve before the irreversible swap)
- Earlier update: 2026-05-26 (B2-12 implementation shipped against ADR-0029 §"row_count_positive cost gap" — the inaugural set-mode kind now adds an optional `WHERE` partition predicate keyed on `source.partition_column` + half-open interval `[window_start, window_end)`; runtime evaluator uses BigQuery parameterized queries (`@window_start` / `@window_end`); `tools/dryrun` mirrors the SQL template + binds placeholder timestamps; `validateBQIdentifiers` extended with a `bqColumnPattern` check; behavior unchanged for rules without `partition_column`; eval + dryrun tests cover both paths)
- Earlier update: 2026-05-26 (B2-11 implementation shipped against ADR-0029 — new `tools/dryrun/` Go module + `dq-dryrun` binary that walks rule YAMLs, compiles SQL for set-mode BigQuery-source kinds (mirrors the runtime evaluator's `set.row_count_positive` template today), issues BigQuery dry-run, reports + enforces `MaxBytesScannedPerRun` ceiling; new `make dry-run-rules` Make target + `.github/workflows/dryrun.yml` CI lane against the local emulator; v1 rules / record-mode / non-bigquery / no-template-yet kinds recorded as skips)
- Earlier update: 2026-05-26 (B2-27 implementation shipped against ADR-0041 — `window_start` / `window_end` columns added to `dq_executions` (Required=true; pre-B2-27 deployments need ALTER TABLE backfill); `ExecutionRow` struct + BigQuery `executionRecord` round-trip both; `runningOlderThanSQL` + `currentExecutionsInlineSQL` SELECT lists extended; three runner.go call sites + orphan's followup-row constructor populate window endpoints; ADR-0039's stable-column inventory amended; full engine test suite + per-module sweep clean; paced ahead of Phase-4c metric emission per operator request)
- Earlier update: 2026-05-26 (B2-32 implementation shipped against ADR-0044 — bounded external-reference resolution end-to-end; new `tools/pathsafe/` shared Go module (three-step Resolve: no-`..` + EvalSymlinks canonicalization + rules-tree containment + absolute-path rejection); catalog v1 extended with `external_eligible_fields: [schema]` + `schema_ref: {type: string}` on `record.schema_conformance` (both canonical and mirror); `dq-lint` cross-check #12 enforces mutual-exclusion + at-least-one + non-eligible-field rejection + read/parse/validate-against-sub-schema; `dq-manifest publish` mirror-resolves at publish time and re-serializes the inlined YAML so the content-addressed body stays self-contained per ADR-0005; seven invalid + one valid lint fixtures + five publisher tests cover the documented cases; full per-module test sweep clean)
- Earlier update: 2026-05-26 (B2-30 implementation shipped against ADR-0042 Clause 4 + W3-P7c — deeper deploy-validation via `kubeconform -strict` layered into the existing `make validate-deploy` target (no new top-level CI lane); `.github/workflows/deploy-validate.yml` installs kubeconform v0.7.0 pinned; smoke-tested with deliberate `replicass:` typo — gate fires with "additional properties 'replicass' not allowed" and exit code 1; the three ADR-0042 implementation slices (B2-28 Dockerfile, B2-29 tag-prefix gate, B2-30 deeper deploy-validation) are now all closed)
- Earlier update: 2026-05-26 (B2-29 implementation shipped against ADR-0042 Clause 3 + W2-5 §C-W2-5.5 — per-workspace tag-prefix CI gate; shared `scripts/check-tag-scope.sh` maps the five committed prefixes to their workspace paths, resolves prior tag via semver-style sort, diffs against current, rejects any out-of-scope path with clear violator list; new `.github/workflows/tag-prefix-gate.yml` triggers on tag-push or `workflow_dispatch`; `make check-tag-scope TAG=<tag>` runs the same script locally; smoke-tested with synthetic tag pair — first-tag passes by inception, violations correctly enumerated)
- Earlier update: 2026-05-26 (B2-28 implementation shipped against ADR-0042 Clause 1 — `engine/Dockerfile` multi-stage build with `golang:1.25-bookworm` builder + `gcr.io/distroless/static-debian12:nonroot` runtime; image runs as `nonroot:nonroot` (UID 65532 matches deployment.yaml pin); root `Makefile` `build-engine-image` target derives tag from git per ADR-0042 Clause 3; new `.github/workflows/engine-image.yml` CI lane builds + inspects on every PR + push (no push — registry choice still TBD per ADR-0042 OQ-1); `engine/go.sum` reconciled via `go mod tidy`; final image size ~61 MB; `deploy/base/deployment.yaml` image marker stays as `dq-engine:placeholder` because the registry prefix is still TBD)
- Earlier update: 2026-05-26 (B2-31 implementation shipped against ADR-0043 — new `engine/internal/logging/` package containing the `DQ_LOG_LEVELS` parser + custom slog Handler with WithAttrs-based `component` capture and longest-prefix-match resolution at dot boundaries; main.go builds per-package loggers via `logger.With("component", "engine.<x>")` and passes each into the matching constructor; startup-time audit log line for ignored package names; smoke-tested with `DQ_LOG_LEVELS=engine.loader:debug,engine.compilers:warn` showing the audit line correctly reports `engine.compilers` as ignored; syntactic errors verified fatal at boot exit 2)
- Earlier update: 2026-05-26 (B2-1 → ADR-0044: external artifact references — bounded external-reference contract for structured-data params; per-field `<field>_ref` suffix on catalog-declared external-eligible fields; publish-time inlining preserves ADR-0005's content-addressed self-contained manifest body; three-step path safety — no `..`, symlink canonicalization, rules-tree containment; permanent SQL/expression prohibition closed by three independent brakes — ADR amendment + catalog schema bump + platform-team CODEOWNERS; no rule-schema bump needed (additive within catalog v1); implementation deferred to new B2-32 slice)
- Earlier update: 2026-05-26 (B2-2 → ADR-0043: logging contract specifics — `DQ_LOG_LEVELS` formalized as five-clause contract (grammar with case-insensitive + whitespace-trim; ten-leaf package inventory aligned to actual `engine/internal/` tree; longest-prefix-match precedence at dot boundaries; asymmetric error handling — syntactic fatal, unknown-name ignored; additive to `EnvConfig.LogLevel`); implementation deferred to new B2-31 slice; foundation 04 PAT-5 shape preserved; ADR-0007 exit posture + ADR-0018 "separate decision" clause both exercised, not amended)
- Earlier update: 2026-05-26 (B2-3 → ADR-0042: release engineering invariants — four-clause cross-workspace contract spanning Docker (Dockerfile path/shape/non-root posture/image-name pattern), Make (per-workspace `lint-<ws>` / `test-<ws>` / `build-<binary>` inventory + aggregator obligation), Versioning (W2-5 prefixes extended with `tools-manifest-v*` + image-tag derivation + immutability), Deploy-validation (deeper lane integrated into `validate-deploy`); closes W2-5 OQ-W2-5.1 + C-W2-5.5 + W3-P7c deferrals; three new B2 rows (B2-28 engine Dockerfile, B2-29 tag-prefix CI gate, B2-30 deeper deploy-validation lane) registered as implementation slices)
- Earlier update: 2026-05-26 (B2-4 → ADR-0041: stream reporting continuity — unified-reporting invariant articulated for set-mode and record-mode under same tables + same `execution_id` scheme; `window_start` / `window_end` committed as additive `dq_executions` columns at contract level; per-mode time-field semantics for `started_at`/`completed_at` committed; cross-mode dashboard interpretation guidelines + mode-transition rule; new B2-27 row registers window-endpoint-column implementation slice paced post-Phase-4c)
- Earlier update: 2026-05-26 (B2-5 → ADR-0040: entity onboarding workflow — three-tier readiness model (Candidate → Test-soak → Production); six-criterion Tier 0 → Tier 1 checklist + seven-criterion Tier 1 → Tier 2 checklist; qa substrate IS the test surface; shared-substrate channel-collision workaround procedurally enforced via criteria 6 + 7; paired runbook `docs/runbooks/entity-onboarding.md` ships alongside; two new B2 rows registered — B2-25 engine-level onboarding-channel mechanism + B2-26 channel-reachability linter extension)
- Earlier update: 2026-05-26 (B2-6 → ADR-0039: dashboard contract — two consumer-surface contracts committed (SQL tables + Prometheus-compatible `/metrics` endpoint); metric inventory formalized from foundation 05 with cardinality/evolution rules; baseline-dashboard implementation deferred as new B2-24 slice paced post-Phase-4c metric emission; three OQs reserved out-of-scope — `dq_entity_rollup` pre-agg, per-metric cardinality ceiling, documented field-level inventory for `evidence_summary` and `sample_violating_rows`)
- Earlier update: 2026-05-25 (B2-7 → ADR-0038: documentation site generator deferred with four auditable trigger conditions — external-audience commitment, navigation-density threshold on `docs/README.md`, retrospective-surfaced search-quality friction, external link-out demand; raw markdown stays the deliverable; ADR-0011 and ADR-0019 preserved)
- Earlier update: 2026-05-25 (B2-9 → ADR-0037: `_owners.yaml` owner-value ↔ CODEOWNERS-group membership cross-check ships as a `dq-lint` extension under a new `-codeowners` flag; closes the timing gap where stale or typo'd owner strings previously only surfaced at PR-review time; two OQs explicitly reserved as future B-items — per-entity CODEOWNERS-path check waits for ADR-0015 §11; email-reviewer support waits for an ADR-0015 §2 amendment)
- Earlier update: 2026-05-25 (B2-10 → ADR-0036: `dq-manifest set-pointer` rollback subcommand committed; runbook §3 rewritten with the new primary path + two demoted fallbacks; one deferred OQ-1 `list-pointers` ergonomic registered for future B-item)
- Earlier update: 2026-05-25 (B1 backlog closed in full — seven B1-tier ADRs landed: B1-2 → ADR-0029, B1-8 → ADR-0030, B1-6 → ADR-0031, B1-1 → ADR-0032, B1-3 → ADR-0033, B1-5 → ADR-0034, B1-7 → ADR-0035; thirteen consumer-slice follow-ups numbered B2-11 through B2-23 from those ADRs; total open B2 backlog now 22 rows)
- Earlier update: 2026-05-23 (W3-P8d closed — Phase 8 closes; B2-9/B2-10 registered from W3-P8b/P8d follow-ups; B1-9 → ADR-0015, B1-10 → ADR-0016, B1-11 → ADR-0017, B1-4 → ADR-0018, B2-8 → ADR-0019; Wave 3 completion gate met)
- Wave-S preparation: 2026-05-23 scope notes applied to ADRs 0002, 0003, 0004, 0006, 0007, 0010, 0014, 0017 declaring set-oriented mode; ADR-0020 (Wave-S launch) lands 2026-05-23 — forward-pointers redeemed.
- Wave-S launch: 2026-05-23 launch study closed at resolved-study (two critique rounds; ref: `studies/decisions/2026-05-23-wave-s-launch.md`) and promoted to [ADR-0020](../../docs/adr/0020-wave-s-launch.md); B0-S1…B0-S7 registered in the Wave-S table below.
- Promotion target: this document stays in `studies/foundation/` for
  the project's lifetime. Resolved decisions are promoted to ADRs
  under `docs/adr/` during Wave 3; rows here keep the link.

---

## Prioritization Model

- **B0** — blocking. Must be resolved before Wave 3 (scaffolding) can
  start. Each B0 gets its own dated decision document in
  `studies/decisions/`.
- **B1** — important. Should be resolved before serious implementation
  starts. Some are refinements of B0 outcomes.
- **B2** — later. Can be resolved as implementation reveals concrete
  needs. Documented here so they are not forgotten.

## Status Vocabulary

- **open** — no work has started.
- **in-progress** — a draft exists in `studies/decisions/` but is not
  yet finalized.
- **resolved-study** — a complete study exists in
  `studies/decisions/`; ready for promotion when Wave 3 starts.
- **resolved-adr** — the study has been promoted to an ADR under
  `docs/adr/`.

---

## B0 — Blocking Decisions (Wave 1 Scope)

| # | Topic | Status | Key Question | Why It Matters | Expected Output |
|---|---|---|---|---|---|
| B0-1 | Engine ↔ rules compatibility | [resolved-study](../decisions/2026-05-20-engine-rules-compatibility.md) → [resolved-adr](../../docs/adr/0001-engine-rules-compatibility.md) | How does the rules workspace declare which schema and linter contract it follows, given both live in the same repository? | Without an explicit contract, the monorepo lets boundaries erode silently. | ADR + boundary contract refinement |
| B0-2 | Run identity and idempotency | [resolved-study](../decisions/2026-05-20-run-identity-and-idempotency.md) → [resolved-adr](../../docs/adr/0002-run-identity-and-idempotency.md) | What uniquely defines a run, and how do reruns of the same window behave? | Reporting trust and alert deduplication depend on it. | Execution semantics ADR |
| B0-3 | Result write model | [resolved-study](../decisions/2026-05-20-result-write-model.md) → [resolved-adr](../../docs/adr/0003-result-write-model.md) | Are `dq_executions` and `dq_check_results` append-only, upserted, or hybrid? | Impacts retries, lineage, and dashboard accuracy. | Storage design ADR |
| B0-4 | Failure scope | [resolved-study](../decisions/2026-05-20-failure-scope.md) → [resolved-adr](../../docs/adr/0004-failure-scope.md) | When one check errors operationally, does the entity error, degrade, or partially complete? | Incidents and alerting become inconsistent without a single policy. | Failure-semantics ADR plus runbook |
| B0-5 | Manifest publication semantics | [resolved-study](../decisions/2026-05-20-manifest-publication-semantics.md) → [resolved-adr](../../docs/adr/0005-manifest-publication-semantics.md) | What guarantees atomic, reversible ruleset publication to object storage? | Runtime safety depends on manifest discipline. | Control-plane contract ADR |
| B0-6 | Alert routing contract | [resolved-study](../decisions/2026-05-21-alert-routing-contract.md) → [resolved-adr](../../docs/adr/0006-alert-routing-contract.md) | What fields live in `_owners.yaml`, what stays in engine config, and what is deduplicated on the data itself? | Prevents alerting logic from becoming hardcoded chaos. | Governance doc + owners schema |
| B0-7 | Loader / scheduler / retry failure semantics | [resolved-study](../decisions/2026-05-20-loader-scheduler-retry-failure-semantics.md) → [resolved-adr](../../docs/adr/0007-loader-scheduler-retry-failure-semantics.md) | What exactly causes ruleset load failure, scheduler reconciliation failure, and retry budget exhaustion? | The fail-fast registry pattern only helps if failure semantics are explicit and consistent. | Loader and scheduler ADRs |

---

## B1 — Important Decisions (Pre-Implementation Scope)

| # | Topic | Status | Key Question | Why It Matters | Expected Output |
|---|---|---|---|---|---|
| B1-1 | Baseline strategy | [resolved-study](../decisions/2026-05-25-b1-1-baseline-strategy.md) → [resolved-adr](../../docs/adr/0032-baseline-strategy.md) | Where do moving averages and historical references come from, and what happens with sparse history? | Volume and freshness checks depend on consistent history semantics. | Check design note |
| B1-2 | BigQuery cost ceilings | [resolved-study](../decisions/2026-05-25-b1-2-bigquery-cost-ceilings.md) → [resolved-adr](../../docs/adr/0029-bigquery-cost-ceilings.md) | What are the per-environment limits for window size, concurrency, failed samples, and dry-run enforcement? | Cost drift is predictable; designing around it is cheap. | Operations doc + defaults policy |
| B1-3 | Scheduler catchup behavior | [resolved-study](../decisions/2026-05-25-b1-3-scheduler-catchup-behavior.md) → [resolved-adr](../../docs/adr/0033-scheduler-catchup-behavior.md) | How are catchup, missed windows, and manual triggers represented? | A scheduler without precise semantics causes duplicate or missing evaluations. | Scheduling design note |
| B1-4 | Environment configuration model | [resolved-study](../decisions/2026-05-22-b1-4-environment-configuration-model.md) → [resolved-adr](../../docs/adr/0018-environment-configuration-model.md) | Which configuration lives in code, deployment, or data, and how are `local`, `qa`, and `prod` isolated? | Prevents configuration sprawl and implicit behavior drift. | Env strategy ADR |
| B1-5 | Local testing strategy | [resolved-study](../decisions/2026-05-25-b1-5-local-testing-strategy.md) → [resolved-adr](../../docs/adr/0034-local-testing-strategy.md) | What can be tested offline, what needs BigQuery sandbox access, and how is generated SQL inspected? | Developer experience shapes long-term quality. | Dev guide + tooling scope |
| B1-6 | Evidence retention parameters | [resolved-study](../decisions/2026-05-25-b1-6-evidence-retention-parameters.md) → [resolved-adr](../../docs/adr/0031-evidence-retention-parameters.md) | How many violating samples per check, for how long, under what privacy constraints? | Storage cost and privacy compliance depend on it. | Storage and security note |
| B1-7 | Compatibility window duration | [resolved-study](../decisions/2026-05-25-b1-7-compatibility-window-duration.md) → [resolved-adr](../../docs/adr/0035-compatibility-window-duration.md) | How long is each schema version supported after its successor is released? | Migration ergonomics for domain teams. | Boundary contract refinement |
| B1-8 | Manifest cryptographic posture | [resolved-study](../decisions/2026-05-25-b1-8-manifest-cryptographic-posture.md) → [resolved-adr](../../docs/adr/0030-manifest-cryptographic-posture.md) | Does the manifest carry signatures beyond checksums? Who signs it? | Defense in depth against tampering or accidental publication. | Security note |
| B1-9 | CODEOWNERS finalization | [resolved-study](../decisions/2026-05-22-b1-9-codeowners.md) → [resolved-adr](../../docs/adr/0015-codeowners.md) | Final team names and path rules for the asymmetric review model. | Enforces the boundary at PR-review time. | CODEOWNERS file |
| B1-10 | Workspace tooling stack | [resolved-study](../decisions/2026-05-21-b1-10-workspace-tooling.md) → [resolved-adr](../../docs/adr/0016-workspace-tooling.md) | Confirm Go workspaces (`go.work`) as the tooling choice and finalize the per-tool module structure. | Affects every CI pipeline file. | Topology ADR refinement |
| B1-11 | ADR-0010 substrate-posture amendment — object-store CAS row | [resolved-study](../decisions/2026-05-21-b1-11-substrate-posture-amendment.md) → [resolved-adr](../../docs/adr/0017-substrate-posture-amendment.md) | The ADR-0010 "Object store: generation-conditional pointer write" row is committed as `Yes`, but Phase 2 emulator evaluation found commodity emulators do not faithfully enforce `ifGenerationMatch` (fake-gcs-server accepts stale-generation writes; storage-testbench requires GCP auth; oittaa lacks the media-upload endpoint). The row should be amended to `Partial` so production-shape CAS enforcement is explicitly sandbox-required, matching the existing pattern for the tabular-store lazy-view row. | Without amendment, the ADR-0010 contract and the deployed `docker-compose.yml` disagree on whether local CAS is faithful. | Amendment ADR (or a Wave 3 follow-up adjusting ADR-0010's matrix row). |

---

## B2 — Later Decisions (Implementation-Phase Scope)

| # | Topic | Status | Key Question | Why It Matters | Expected Output |
|---|---|---|---|---|---|
| B2-1 | External artifact references in DSL | [resolved-study](../decisions/2026-05-26-b2-1-external-artifact-references.md) → [resolved-adr](../../docs/adr/0044-external-artifact-references.md) | Will the DSL ever allow helper files (auxiliary SQL, reference payloads) resolved relative to the rule origin? | The capability is useful but dangerous if it becomes an escape hatch. | DSL evolution ADR |
| B2-2 | Logging contract specifics | [resolved-study](../decisions/2026-05-26-b2-2-logging-contract-specifics.md) → [resolved-adr](../../docs/adr/0043-logging-contract-specifics.md) | Which package names and override syntax are officially supported by `DQ_LOG_LEVELS`? | Good observability patterns help only if standardized. | Operations doc |
| B2-3 | Release engineering invariants | [resolved-study](../decisions/2026-05-26-b2-3-release-engineering-invariants.md) → [resolved-adr](../../docs/adr/0042-release-engineering-invariants.md) | Which Docker, Make, and versioning invariants are mandatory across all workspaces? | Long-term repo health depends on consistent ergonomics. | Release engineering doc |
| B2-4 | Stream reporting continuity | [resolved-study](../decisions/2026-05-26-b2-4-stream-reporting-continuity.md) → [resolved-adr](../../docs/adr/0041-stream-reporting-continuity.md) | How will stream-runner results align with batch result tables and identifiers? | Future migration should not fracture observability. | Stream evolution design note |
| B2-5 | Entity onboarding workflow | [resolved-study](../decisions/2026-05-26-b2-5-entity-onboarding-workflow.md) → [resolved-adr](../../docs/adr/0040-entity-onboarding-workflow.md) | What exact checklist determines when a new entity is ready for test channel and later for production alerting? | Governance quality depends on repeatable onboarding. | Runbook + checklist |
| B2-6 | Dashboard contract | [resolved-study](../decisions/2026-05-26-b2-6-dashboard-contract.md) → [resolved-adr](../../docs/adr/0039-dashboard-contract.md) | Which metrics and dimensions are guaranteed for downstream consumers (Looker, Grafana, etc.)? | Avoids each consumer inventing its own interpretation. | Reporting contract |
| B2-7 | Documentation site generator | [resolved-study](../decisions/2026-05-25-b2-7-documentation-site-generator.md) → [resolved-adr](../../docs/adr/0038-documentation-site-generator.md) | Does `docs/` get a static site generator, or stay as raw markdown? | Affects how documentation is discovered by non-developers. | Documentation infrastructure note |
| B2-8 | Infrastructure tooling | [resolved-study](../decisions/2026-05-22-b2-8-infrastructure-tooling.md) → [resolved-adr](../../docs/adr/0019-infrastructure-tooling.md) | Kustomize, Helm, Terraform, or a combination for `deploy/`? | Affects deployment ergonomics and environment isolation. | Infrastructure ADR |
| B2-9 | Owner ↔ CODEOWNERS-group linter cross-check | [resolved-study](../decisions/2026-05-25-b2-9-owner-codeowners-cross-check.md) → [resolved-adr](../../docs/adr/0037-owner-codeowners-cross-check.md) | Should `dq-lint` parse `.github/CODEOWNERS` and reject `_owners.yaml` entries whose `owner:` does not correspond to an existing CODEOWNERS group? | ADR-0006 §9 commits the linter as the first enforcement point for "no alert without owner"; without the cross-check, a stale or typo'd group reference only fails at PR-review time. Defense-in-depth complement to OQ-B1-9.3 (publisher/loader-side validation). Flagged when W3-P8b closed. | Linter rule design note + `dq-lint` extension |
| B2-10 | `dq-manifest set-pointer` rollback subcommand | [resolved-study](../decisions/2026-05-25-b2-10-set-pointer-subcommand.md) → [resolved-adr](../../docs/adr/0036-set-pointer-subcommand.md) | Should `dq-manifest` expose a first-class `set-pointer <hash>` subcommand to execute the rollback procedure in `docs/runbooks/manifest-rollback.md`? Today the CLI exposes only `publish`, and operators fall back to `gsutil`/console writes that bypass CLI contract enforcement. | Rollback ergonomics during incident response; the runbook's §2 procedure is TBD-blocked on this CLI surface. Flagged when W3-P8d closed. | CLI design note + subcommand under `tools/manifest/` |
| B2-11 | `tools/dryrun` binary | resolved (implementation shipped against ADR-0029) | Should a new CLI binary issue a BigQuery dry-run against each rule's compiled query template and post the bytes-scanned estimate per PR? | ADR-0029 §"Compiler layer" committed the posture; implementation landed: new `tools/dryrun/` Go module with `Runner` type that walks the rules dir, compiles SQL for set-mode bigquery-source kinds (today's only such kind is `set.row_count_positive`, template mirrors the runtime evaluator at `engine/internal/eval/row_count_positive.go`), issues BigQuery dry-run, reports per-rule estimates + aggregate total bytes, enforces a configurable `MaxBytesScannedPerRun` ceiling. CLI exit codes follow the standard `tools/*` contract (0/1/2/64). New `make dry-run-rules` target replaces the prior stub; `make build-dryrun` builds the binary. New `.github/workflows/dryrun.yml` lane brings up the local emulator + runs the binary on every PR + push; emulator dry-run fidelity is best-effort (SQL-syntax + source-existence caught faithfully; bytes-scanned not authoritative until CI gets real-BQ creds). v1 rules, record-mode, non-bigquery sources, and kinds without compiler-side templates are recorded as skips. | New `tools/dryrun/` Go module + `Makefile` `build-dryrun` + `dry-run-rules` real target + `.github/workflows/dryrun.yml` + go.work extension |
| B2-12 | `row_count_positive` partition-filter retrofit | resolved (implementation shipped against ADR-0029) | Should the inaugural set-mode kind grow partition-pruning logic keyed on `Source.PartitionColumn` (or ship a new partition-aware kind alongside)? | ADR-0029 §"row_count_positive cost gap" — committed the cost gap; the retrofit was deferred to a follow-up. Implementation landed via the **enrichment** branch (Option A): the existing `set.row_count_positive` kind now emits an additional `WHERE \`<partition_column>\` >= @window_start AND \`<partition_column>\` < @window_end` predicate when `source.partition_column` is set, using parameterized BigQuery query parameters; behavior unchanged for rules without `partition_column`. `engine/internal/eval/row_count_positive.go`'s SQL template centralized in a new `rowCountPositiveSQL(tableRef, partitionColumn)` helper; `tools/dryrun/dryrun.go` mirrors the same template + binds placeholder timestamps so the PR-time dry-run estimates the same bytes the runtime evaluator would; `validateBQIdentifiers` extended with a `bqColumnPattern` check for `PartitionColumn` (defense-in-depth against direct CheckSpec construction). Half-open interval `[window_start, window_end)` matches the runner's `WindowEnd > WindowStart` validateTrigger precondition. Partition column type assumed TIMESTAMP-compatible (TIMESTAMP/DATETIME/DATE); future ADR amendment can extend the source descriptor with an explicit type tag if other types surface operationally. | `engine/internal/eval/row_count_positive.go` enrichment + `tools/dryrun/dryrun.go` mirror + unit tests on both sides |
| B2-13 | Pre-existing-table partitioning migration runbook | resolved (implementation shipped against ADR-0031) | Future deployments that have accumulated non-partitioned `dq_executions` + `dq_check_results` history before ADR-0031 reaches them require a one-time `CREATE TABLE ... AS SELECT ... PARTITION BY DATE(recorded_at)` + rename migration. What does the operator runbook look like? | ADR-0031 — committed the migration as a B2 follow-up; implementation landed as `docs/runbooks/results-partition-migration.md` with the standard six-section anatomy (when to use → preconditions → procedure → verification → rollback → escalation). Procedure covers both tables (dq_executions partitioned by `recorded_at`; dq_check_results by `executed_at` per ADR-0003 §7 column inventory); explicit row-count verification before the destructive swap; engine-pause precondition; snapshot-based escape valve before the irreversible step. | New runbook at `docs/runbooks/results-partition-migration.md` + runbooks README index update |
| B2-14 | First baselined kind | resolved (implementation shipped against ADR-0032) | Which baselined-check kind ships first under ADR-0032's framework — `set.row_count_within_baseline`, `set.freshness_within_baseline`, or another? The slice ships the `params.baseline` schema fragment + the `ComputeBaseline` helper + the new kind's handler + catalog entry together. | ADR-0032 §"Why this does NOT commit specific baselined kinds" — the framework is design-only; the first consumer slice implements it. Implementation landed: chose `set.row_count_within_baseline` as the inaugural baselined kind (numeric-aggregate kind, smallest possible diff against the established `set.row_count_positive` flow). New `engine/internal/eval/baselines.go` carries the framework — `BaselineSpec`/`Tolerance`/`BaselineResult` types, `ParseReferenceWindow` (closed-grammar parser for the `[0-9]+(ms|s|m|h|d)` literal), `ParseBaselineSpec` (both `platform_history` and `static` sub-modes with closed-enum guard for aggregation/tolerance), `ComputeBaseline` (parameterised BQ query against `dq_check_results`+`dq_executions` joined on `(execution_id, attempt_id)`, reading only `pass` rows, capped by `min(declared, ResultsRetention)`), and `aggregationExpr` (BigQuery `APPROX_QUANTILES(col, 100)[OFFSET(N)]` idiom for percentiles; bare AVG/MIN/MAX for the others). New `engine/internal/eval/row_count_within_baseline.go` carries the five-step handler (parse baseline → current-value count reusing `rowCountPositiveSQL` so partition pruning from B2-12 is inherited automatically → ComputeBaseline → sparse-history → ResultDegraded with `insufficient_baseline_samples` reason → pass/fail by tolerance). `EnvConfig.EvidenceRetention.ResultsRetention` wired (30d/90d/365d for local/qa/prod per ADR-0031); `eval.Config` extended with `ResultsProject`/`ResultsDataset`/`ResultsRetention` and threaded through `cmd/dq-engine/main.go`. Catalog entry added to `engine/internal/dsl/catalog/v1.yaml` + mirror with full `params_schema` for the baseline block. `tools/dryrun` extended to match the new kind against the same current-value SQL template (baseline query itself is NOT dry-run-checked — `dq_check_results` may not exist in emulators; runtime evaluator runs it independently per ADR-0032). | New `engine/internal/eval/baselines.go` framework + `engine/internal/eval/row_count_within_baseline.go` handler + catalog entry + `EnvConfig.EvidenceRetention` + `tools/dryrun` mirror + unit tests on the helpers |
| B2-15 | Baselined-check degraded runbook seed | resolved (implementation shipped against ADR-0032) | What does the operator do when a baselined check fires `degraded` with `reason: insufficient_baseline_samples`? | ADR-0032 §"Sparse-history policy" — the degraded status is committed; the runbook clarifying the response posture lands with the first baselined kind. Implementation landed: new operator runbook `docs/runbooks/baselined-check-degraded.md` with the standard six-section anatomy. Section 3.B walks four causes ordered by likelihood — (1) new check still warming up, (2) declared `reference_window` exceeds env `ResultsRetention` (cap fired per ADR-0032 §"Effective reference window vs declared"), (3) `min_samples` set too high for the check's cadence, (4) recent `fail` / `error` rows shrank the `pass` set the baseline query reads. Each cause has a confirmation SQL query + remediation (wait / edit rule via the PR → publish path / fix underlying check). Section 5 explicitly notes degraded is itself the safe state — no destructive rollback. Section 6 routes the four escalation conditions (qa/prod retention tension, sustained fail, missing `effective_reference_window`, baseline-query bug) to platform-team or entity owner per ADR-0006 routing. Maturity disclaimer + stddev-tolerance v1-placeholder note included. README runbook index extended with the new signal row. | New runbook at `docs/runbooks/baselined-check-degraded.md` + README index update |
| B2-16 | First scheduler-consumer slice | resolved (implementation shipped against ADR-0033) | Which external scheduler is the reference consumer of ADR-0033's `schedule` field + `EnvConfig.SchedulerCatchupHorizon` + `LatestExecutionPerEntityCheck` reader? Likely a Kubernetes CronJob manifest under `deploy/overlays/` that reads the manifest at publication time and provisions cron entries. | ADR-0033 — the scheduler is external; the framework is design-only; the first consumer slice ships the reference. Implementation landed: (1) optional `schedule` field added to v2 rule schema with `oneOf` gating duration-literal `^[0-9]+(ms|s|m|h|d)$` and 5-field cron `^\\S+ \\S+ \\S+ \\S+ \\S+$` grammars (canonical + mirror; byte-equal via `make sync-schema`); (2) new `EnvConfig.SchedulerCatchupHorizon time.Duration` field populated 1h / 6h / 24h for local / qa / prod (advisory per ADR-0033 §"Per-env SchedulerCatchupHorizon"; engine doesn't enforce); (3) new `Store.LatestExecutionPerEntityCheck(asOf time.Time)` reader method + `LatestExecutionRow` type — partition-pruned BQ query joining `dq_executions` + `dq_check_results` and reducing to one row per `(entity, check_id)` keyed on latest `window_end ≤ asOf`; BigQueryStore impl + mockStore + inMemStore + unit test covering the asOf cutoff; (4) reference Kubernetes CronJob at `deploy/base/cronjob-scheduler.yaml` — illustrative example POSTing to `/v1/trigger` per ADR-0014, heavy comments on the operator workflow (copy → parameterise entity + window + schedule; per-env startingDeadlineSeconds matches SchedulerCatchupHorizon), placeholder image `curlimages/curl:8.11.0`, included in `deploy/base/kustomization.yaml`, deploy-validate green (5 resources per overlay now). Lint fixtures cover both schedule forms (valid duration + valid cron) and the malformed 4-field cron (invalid). The runbook seed for `manual` vs `operator-rerun` (B2-17) lands separately. | New `schedule` field on v2 schema + canonical/mirror sync + `EnvConfig.SchedulerCatchupHorizon` + `Store.LatestExecutionPerEntityCheck` reader + reference Kubernetes CronJob manifest + lint fixtures |
| B2-17 | Manual-vs-operator-rerun runbook seed | resolved (implementation shipped against ADR-0033) | When does an operator use `trigger_source: manual` vs `trigger_source: operator-rerun`? Distinct execution_ids per ADR-0002 CC1, but the operational guidance lives in a runbook. | ADR-0033 §"Manual trigger semantics" — committed the distinction; the runbook seed lands with the first scheduler-consumer slice. Implementation landed: new operator runbook `docs/runbooks/manual-vs-operator-rerun.md` with the standard six-section anatomy. Section 3.A walks a three-question decision tree (replace prior result → operator-rerun; parallel observation → manual; uncovered window → manual). Section 3.B documents the manual `/v1/trigger` call shape (RFC 3339 Z-only window strings per ADR-0014 §2). Section 3.C explicitly flags that the **Admin API for operator-rerun is not yet implemented at v1** — documents the eventual call shape (mandatory prior execution_id; supersedes_execution_id auto-populated per ADR-0003 §5) plus three concrete workarounds (wait for next scheduler tick; manual + accept missing lineage; out-of-band ticket) until the Admin API B2 follow-up lands. Section 3.D supplies a `dq_executions` read pattern for distinguishing the four row shapes (retries vs parallel observations vs supersede chains). Section 5 confirms append-only contract — wrong results don't roll back; another rerun extends the supersede chain. Section 6 routes three escalation conditions (Admin API gap; status=error on accepted trigger; orphan supersedes_execution_id pointer) to platform-team or entity owner. Maturity disclaimer flags the Admin API as the load-bearing TBD. README runbook index extended with the new signal row. | New runbook at `docs/runbooks/manual-vs-operator-rerun.md` + README index update |
| B2-18 | Integration-sandbox tier first consumer slice | resolved (implementation shipped against ADR-0034) | What's the first test that lands under the `//go:build sandbox` tag, and what `test-engine-sandbox` make target ships alongside? | ADR-0034 §"`integration-sandbox`" reserved the tier at v1; the first consumer slice lands with the operational session that provisions real GCP (and addresses Partial-row capabilities — CAS fidelity, lazy view, broker-set-timestamp watermark). Implementation landed: chose **object-store CAS fidelity** as the first sandbox capability (ADR-0017 Partial row — fake-gcs-server accepts `ifGenerationMatch` query parameters without rejecting stale-generation writes; the publisher's CAS race-loser contract was only ever exercised against the in-mem fake in `publisher_test.go`). New `tools/manifest/publisher_sandbox_test.go` with `//go:build sandbox` carries two functions — `TestSandbox_CASWritePointer_HappyPath` (smoke; real GCS returns non-zero post-write generation for both `DoesNotExist` and `GenerationMatch` branches) and `TestSandbox_CASWritePointer_StaleGenerationRejected` (load-bearing; advances pointer G1 → G2 then attempts stale-G1 CAS write; real GCS returns HTTP 412 mapped to `ErrPreconditionFailed`). Tests gate execution on `DQ_SANDBOX_PROJECT` + `DQ_SANDBOX_BUCKET` env vars and `t.Skip` cleanly when unset, so `make test-sandbox` is a no-op without sandbox credentials. Three new Make targets — `test-tools-manifest-sandbox` (runs the manifest sandbox tests directly), `test-engine-sandbox` (reserved no-op until engine-side sandbox tests land), `test-sandbox` (umbrella across all modules). New `.github/workflows/sandbox.yml` CI lane — `workflow_dispatch` only at v1 (no `pull_request` / `push` trigger; explicitly NOT gating the default PR lane per ADR-0034); gated on `vars.SANDBOX_ENABLED == 'true'` or presence of `DQ_SANDBOX_PROJECT` secret; weekly `schedule` trigger committed but commented out until GCP credentials are stable. `docs/dev/local-testing.md` §"integration-sandbox" flipped from "reserved at v1" to documenting the landed test (the two functions, the env-var contract, the three Make targets, the CI lane status, the cost-discipline rationale for not gating PR lane). | First `//go:build sandbox` test at `tools/manifest/publisher_sandbox_test.go` + three Make targets + `.github/workflows/sandbox.yml` + dev guide update |
| B2-19 | `customer.yaml` v1 → v2 migration | resolved (implementation shipped against ADR-0035) | When does `rules/customer.yaml` migrate from `version: 1` to `version: 2` (gains `mode: set` + `source: { type: bigquery, ... }` + `kind: set.row_count_positive`)? | ADR-0035 §"Current state and v1 retirement sequencing" — the Wave-S β slice's "Option (b) — defer rule migration" left customer.yaml on v1; v1 retirement (B2-20) gates on this migration first. Implementation landed: `rules/customer.yaml` migrated atomically to v2 with `mode: set` (ADR-0021 §C-B0S1.3), explicit `source: {type: bigquery, project_id: dq-local, dataset_id: dq_fixture, table_id: customer}` matching the demo-substrate seed (ADR-0023), and kind prefix `set.row_count_positive` (ADR-0022). `scripts/smoke/demo-p6.sh` publish call extended with `-supported-schema-versions "1,2"` so the publisher accepts the v2 manifest; engine main.go already declared `{1, 2}` (no engine change required). Lint clean (`dq-lint -rules rules`); per-module test sweep clean (engine + tools/lint + tools/manifest + tools/dryrun + tools/pathsafe); `make validate-deploy` clean (5 resources per overlay unchanged); `make dry-run-rules` against the local BigQuery emulator now exercises customer.yaml as a v2 set-mode rule (previously skipped by `tools/dryrun` since v1 has no source descriptor) — emulator returns bytes=0 (lenient dry-run; figure unreliable per the documented gap). `rules/_owners.yaml` is unchanged — `customer` entity already declares `mode: set`. Unblocks **B2-20 (v1-retirement engine release)** alongside the 90-day floor (earliest 2026-08-23). Pre-existing scope notes: `engine/internal/api/demo_p6_integration_test.go` has a `//go:build integration` vet failure unrelated to B2-19 (references `eval.Config.SourceProject` / `SourceDataset` removed by ADR-0023); no CI lane currently exercises integration-tagged tests so the failure is masked, but should be tracked in a future cleanup slice. | Migrated `rules/customer.yaml` v2 + demo-p6 publish flag update |
| B2-20 | v1-retirement engine release | open | When does the engine release that drops v1 from `SupportedSchemaVersions` ship? Gates on the 90-day floor (earliest 2026-08-23) AND B2-19 (customer.yaml migrated). | ADR-0035 §"Drop mechanism" + §"Current state" — bounded engine complexity (P4); operators rely on the 90-day floor for migration planning (P5). Flagged when ADR-0035 was promoted (B1-7 closure). | Engine release with `SupportedSchemaVersions: {2}` + v1 parser/schema/mirror/fixture removal + engine version bump + compatibility-state table amendment |
| B2-21 | `tools/lint` deprecation warning | resolved (implementation shipped against ADR-0035) | Should `dq-lint` emit a deprecation warning when a rule's `version` field declares a schema version marked deprecated in ADR-0035's compatibility-state table? Warning while deprecated; hard error at the version's drop release. | ADR-0035 §"Migration support level" — pairs with the v1-retirement engine release (B2-20) to give rule authors visible signal during the 90-day floor. Implementation landed: new `tools/lint/compatibility.go` declares the ADR-0035 compatibility-state table as a Go const map (`SchemaCompatibility`) — v1 marked `deprecated` with earliest-drop `2026-08-23`; v2 marked `current` with TBD drop. Helper `SchemaVersionStatus(version)` returns the entry. New walker `CheckDeprecatedSchemaVersions(dir)` reuses `parseRuleVersion` + mirrors the existing `ValidateRulesDir` walker posture (skips `_schema/` subtree, skips `_`-prefixed files, treats missing dir as empty). Returns `[]DeprecationWarning{Path, Version, Message}` — the message cites ADR-0035 + engine-support-since + earliest-drop date so operators can audit at a glance. `tools/lint/main.go` wires it after `ValidateRulesDir`: warnings are emitted to stderr with `DEPRECATED:` prefix before any validation errors and do **not** influence the exit code (warnings are informational). New `-no-deprecation-warnings` flag (default `false`) suppresses the surface when the noise isn't wanted. Drop-release hard-error behavior remains B2-20's territory: when v1 is removed from the engine's `SupportedSchemaVersions`, the schema-version dispatcher rejects v1 at validation time before reaching this helper, so the same compatibility-state map can be repurposed at that time (or kept as a historical lookup). Unit tests cover the table-keyed lookup, the walker's positive path (v1 fixture fires), the walker's negative path (v2 fixtures don't), the `_schema/` directory skip, the `_`-prefixed file skip, and the missing-dir graceful-empty return. The customer migration shipped by B2-19 means the production `rules/` directory currently emits no warnings; the testdata v1 fixture (`tools/lint/testdata/valid/customer.yaml`) is the live exercise surface. | `tools/lint/compatibility.go` + main.go wiring + unit tests |
| B2-22 | `docs/dev/schema-migration.md` playbook | open | Should a dedicated dev guide consolidate the v1 → v2 migration delta (and future v(N) → v(N+1) deltas) as a single operator-readable playbook? | ADR-0035 §"Migration support level" — documentation-grade migration guidance; lands under `docs/dev/` (introduced by ADR-0034). Flagged when ADR-0035 was promoted (B1-7 closure). | New guide under `docs/dev/` |
| B2-23 | `tools/migrate` binary | open | Should an automated rule-migration tool ship that emits the migrated YAML for `tools/migrate -from=v1 -to=v2 rules/customer.yaml`? Would handle field renames + structural transforms. | ADR-0035 §"Migration support level" — operator ergonomic; the v1 escape hatch is engine pinning, so the tool is nice-to-have not load-bearing. Flagged when ADR-0035 was promoted (B1-7 closure). | New binary under `tools/migrate/` |
| B2-24 | Baseline dashboard consumer slice | open | Implement the example consumer dashboard committed by foundation 05 §"Dashboards" (run success rate, per-entity check pass/fail rates, cost per entity per day, alerting volume per owner, scheduler health) against ADR-0039's contract. The slice's ADR commits the substrate choice and the workspace placement (likely `deploy/dashboards/` or a new `dashboards/` workspace per ADR-0019). | ADR-0039 §"Baseline-dashboard implementation — deferred" — the contract is committed; the example consumer slice paces post-Phase-4c metric emission so smoke-tests can verify end-to-end. Flagged when ADR-0039 was promoted (B2-6 closure). | Dashboard implementation under `deploy/dashboards/` (or new `dashboards/` workspace) + new ADR committing substrate + queries |
| B2-25 | Engine-level onboarding-channel mechanism | open | Should the engine ship a per-entity `onboarding: true` flag (`_owners.yaml` v3) + `EnvConfig.OnboardingChannel` override that routes Tier 1 alerts to a fixed onboarding channel regardless of `_owners.yaml.channels`? Closes the shared-substrate channel-collision workaround procedurally enforced by ADR-0040's Tier 0 → Tier 1 criterion 6 + Tier 1 → Tier 2 criterion 7. | ADR-0040 §"Open Questions OQ-1" — the v1 procedural workaround (qa-prefix at Tier 1; rename at Tier 2) requires a two-step `_owners.yaml` edit per onboarding for shared-substrate deployments. Engine-level mechanism would close this with one flip of a boolean. Flagged when ADR-0040 was promoted (B2-5 closure). | `_owners.yaml` v3 schema + `EnvConfig` extension + engine routing logic + ADR-0006 amendment |
| B2-26 | Channel-reachability linter extension | open | Should `dq-lint` extend to ping Slack-API / SMTP / PagerDuty-API to verify that destinations referenced in `_owners.yaml.entities.<entity>.channels` actually exist? Automates ADR-0040's Tier 1 → Tier 2 criterion 4 (currently manual). | ADR-0040 §"Open Questions OQ-2" — substrate access from the linter is out of scope per ADR-0034's local-testing posture, so this requires committing the substrate-access posture for the linter (a new ADR). The manual check is adequate for v1 onboarding cadence; this is a follow-up when concrete signal demonstrates the manual check is too cumbersome. Flagged when ADR-0040 was promoted (B2-5 closure). | `tools/lint` extension + new substrate-access ADR or extension of ADR-0034 |
| B2-27 | Window-endpoint columns implementation slice | resolved (implementation shipped against ADR-0041) | Implement the additive `window_start` / `window_end` columns on `dq_executions` committed by ADR-0041 at the contract level. Adds `WindowStart` / `WindowEnd` to `engine/internal/results/types.go`'s `ExecutionRow`, ships the DDL migration, updates the `dq_executions_current` view projection, and amends ADR-0039's stable-column inventory. | ADR-0041 §"Window-endpoint columns (design-only)" — contract was binding; implementation landed: `ExecutionRow` gains `WindowStart` + `WindowEnd time.Time` fields; `executionRecord` (BigQuery struct) + `toExecutionRecord` + `fromExecutionRecord` round-trip both; `executionsSchema()` declares both columns Required=true (pre-B2-27 deployments need ALTER TABLE backfill per the schema.go package doc); `runningOlderThanSQL` + `currentExecutionsInlineSQL` SELECT lists extended; three runner.go call sites (`buildRunningRow`, `writePreCheckErrorRow`, `writeTerminalRow`) populate from `TriggerRequest.WindowStart` / `WindowEnd` (already available since Wave-S); orphan's `buildFollowupRow` preserves them from the candidate row; ADR-0039's stable-column inventory amended with the new entries; full engine test suite + per-module sweep clean. Paced ahead of Phase-4c metric emission as the operator requested — smoke-test against actual metric output happens in the next slice that wires emission. | `engine/internal/results/types.go` + `engine/internal/results/schema.go` + `engine/internal/results/bigquery_store.go` + `engine/internal/runner/runner.go` (3 call sites) + `engine/internal/orphan/orphan.go` + `docs/adr/0039-dashboard-contract.md` inventory amendment |
| B2-28 | Engine Dockerfile | resolved (implementation shipped against ADR-0042 Clause 1) | Ship `engine/Dockerfile` satisfying ADR-0042 Clause 1 invariants (multi-stage build; non-root + readOnlyRootFilesystem + dropped caps + RuntimeDefault seccomp; image name `dq-engine:<tag>` derived mechanically from `engine-v<tag>` git tag). Closes the `dq-engine:placeholder` forward reference from W3-P7b. | ADR-0042 §"Clause 1 — Docker invariants" + Consequence #7 — contract was binding; implementation landed: `engine/Dockerfile` uses `golang:1.25-bookworm` for the builder stage (matches `engine/go.mod`'s toolchain directive) + `gcr.io/distroless/static-debian12:nonroot` (UID 65532, matches `deploy/base/deployment.yaml`'s `runAsUser` pin) for the runtime stage; static binary built with `CGO_ENABLED=0` + `-trimpath` + `-ldflags="-s -w"`; image runs `nonroot:nonroot` with the dq-engine binary as ENTRYPOINT; final size ~61 MB. Root `Makefile` gains `build-engine-image` target that derives the tag from git per ADR-0042 Clause 3 (`engine-v<tag>` exact-match → `<tag>`; falls back to short SHA when no release tag points at HEAD). New CI workflow `.github/workflows/engine-image.yml` builds + inspects the image on every PR + push (no push to a registry — choice still TBD per ADR-0042 OQ-1). `engine/go.sum` reconciled via `go mod tidy` (workspace mode was masking a transitive entry). | `engine/Dockerfile` + root `Makefile` `build-engine-image` target + `.github/workflows/engine-image.yml` + `engine/go.sum` reconciliation |
| B2-29 | Per-workspace tag-prefix CI gate | resolved (implementation shipped against ADR-0042 Clause 3 + W2-5 §C-W2-5.5) | Ship the CI lane that rejects a push of `<workspace>-v*` whose diff-from-prior-tag touches files outside `<workspace>/` (e.g., `engine-v*` touching `rules/`). Closes W2-5 §C-W2-5.5 + ADR-0042 Clause 3. | ADR-0042 §"Clause 3 — Versioning invariants" — contract was binding; implementation landed: shared shell script at `scripts/check-tag-scope.sh` maps prefix → workspace path (engine-v → engine/, rules-v → rules/, tools-lint-v → tools/lint/, tools-manifest-v → tools/manifest/, deploy-v → deploy/), resolves prior tag via `git tag --sort=-v:refname` (semver order, correct for backport scenarios), diffs `git diff --name-only <prior> <tag>`, rejects on any out-of-scope path. First-tag-of-prefix passes by inception. New workflow `.github/workflows/tag-prefix-gate.yml` triggers on tag push + `workflow_dispatch`; `make check-tag-scope TAG=<tag>` runs the same script locally. Smoke-tested with synthetic tag pair: violations correctly enumerated; first-tag-only path passes. | `scripts/check-tag-scope.sh` + `.github/workflows/tag-prefix-gate.yml` + `Makefile` `check-tag-scope` target |
| B2-30 | Deeper deploy-validation lane integrated into `validate-deploy` | resolved (implementation shipped against ADR-0042 Clause 4 + W3-P7c) | Extend `make validate-deploy` with a deeper-validation lane (e.g., `kubeconform`, kind-based cluster, or equivalent) that catches field-name typos, deprecated API versions, and schema compliance against the Kubernetes API surface. Specific tool decided by the slice. Closes W3-P7c's deferral + ADR-0042 Clause 4. | ADR-0042 §"Clause 4 — Deploy-manifest deeper-validation posture" — contract was binding; implementation landed: `kubeconform` chosen (cluster-free, fast, validates against Kubernetes API schemas including strict mode for unknown-field rejection). `make validate-deploy` now pipes each `kubectl kustomize` overlay through `kubeconform -strict -summary`; missing kubeconform produces a helpful error with install hints. `.github/workflows/deploy-validate.yml` gains an Install kubeconform step pinned at v0.7.0; the same `make validate-deploy` target runs in CI without introducing a new top-level lane. Smoke-tested: clean overlays pass with `Valid: 4, Invalid: 0`; deliberate `replicass:` typo fires the gate with a clear "additional properties 'replicass' not allowed" message and exit code 1. | `Makefile` `validate-deploy` extension + `.github/workflows/deploy-validate.yml` kubeconform install step |
| B2-31 | `DQ_LOG_LEVELS` per-package override implementation | resolved (implementation shipped against ADR-0043) | Implement the per-package logging-override mechanism committed by ADR-0043: parse `DQ_LOG_LEVELS` into the startup config; wire a custom slog handler that resolves a `component` attribute on each log call against the map via longest-prefix-match at dot boundaries; adopt `slog.With("component", "engine.<name>")` as the per-package instantiation convention across the engine's `internal/` packages; ship the startup-time audit log line for ignored-package entries; cover grammar (case-insensitive, whitespace-trim), precedence, error handling, defaults in unit tests. | ADR-0043 §"Implementation posture (deferred)" — contract was binding; implementation landed as the new `engine/internal/logging/` package (parser + handler + tests) + main.go wiring building per-package loggers via `logger.With("component", ...)` and passing each into the matching constructor; smoke-tested end-to-end. The `LogLevels` map lives on `startupConfig` (where the existing emulator-host overrides live) rather than `EnvConfig` because the per-env baseline pattern stayed hardcoded. | `engine/internal/logging/` package + main.go wiring (no env-config struct change beyond the existing pattern) |
| B2-32 | External-artifact-reference implementation slice | resolved (implementation shipped against ADR-0044) | Implement the bounded external-reference contract from ADR-0044: extend catalog v1 with `external_eligible_fields` and add `schema_ref` sibling to `record.schema_conformance`'s `params_schema`; extend `dq-manifest publish` to walk check params, run the three-step path-safety check (no-`..`, symlink canonicalization, rules-tree containment), read + parse + validate + inline at publish time; extend `dq-lint` with the same resolution plus lint cross-check #12 (mutual-exclusion of `<field>` and `<field>_ref`); ship a shared `pathsafe` helper; cover nine documented test fixtures. | ADR-0044 §"Clause 6 — Implementation deferred" — contract was binding; implementation landed: new `tools/pathsafe/` shared module (three-step Resolve: no-`..` + EvalSymlinks canonicalization + rules-tree containment; also rejects absolute paths); catalog v1 extended (both canonical and mirror) with `external_eligible_fields: [schema]` + `schema_ref: {type: string}`; `tools/lint/external_refs.go` implements cross-check #12 (mutual-exclusion + at-least-one + non-eligible rejection + read + parse + validate against field sub-schema); `tools/manifest/external_refs.go` mirrors the resolution for publish-time inlining (rule body re-serialized so manifest stays self-contained per ADR-0005); seven invalid + one valid lint fixture cover the documented cases (valid_ref, both_present, upward_traversal, missing_file, non_eligible, malformed_json, validation_failure, plus the `params-missing-required` fixture repurposed as cross-check #12's at-least-one branch); publisher tests cover happy-path inlining, both-present rejection, non-eligible rejection, upward-traversal rejection, and same-input-determinism per ADR-0005. | New `tools/pathsafe/` Go module + go.work entry + `tools/lint/external_refs.go` + cross-check #12 tests + `tools/manifest/external_refs.go` + publish-time inlining tests + catalog v1 extensions (canonical + mirror) |

---

## Wave 2 — Platform Decisions (Single Consolidated Document)

These are not in the priority backlog because they are a separate
wave with a single decision document covering all of them. They are
listed here so the log is complete.

| # | Topic | Status |
|---|---|---|
| W2-1 | Git host choice (affects CI artifact location and syntax) | [resolved-study](../decisions/2026-05-21-platform-decisions-wave2.md) → [resolved-adr](../../docs/adr/0008-git-host.md) |
| W2-2 | Multi-agent contract — finalize `.claude/`, `.codex/`, `AGENTS.md` | [resolved-study](../decisions/2026-05-21-platform-decisions-wave2.md) → [resolved-adr](../../docs/adr/0009-multi-agent-contract.md) |
| W2-3 | Docker Compose local scope — which services emulated, which sandboxed | [resolved-study](../decisions/2026-05-21-platform-decisions-wave2.md) → [resolved-adr](../../docs/adr/0010-substrate-posture.md) |
| W2-4 | Documentation language policy (English / Portuguese / mixed) | [resolved-study](../decisions/2026-05-21-platform-decisions-wave2.md) → [resolved-adr](../../docs/adr/0011-documentation-language.md) |
| W2-5 | Per-workspace tag prefix conventions (confirm or revise) | [resolved-study](../decisions/2026-05-21-platform-decisions-wave2.md) → [resolved-adr](../../docs/adr/0012-tag-conventions.md) |

---

## Wave 3 — Phases (Scaffolding Sequencing)

Phase structure committed in
[`studies/decisions/2026-05-21-wave3-sequencing.md`](../decisions/2026-05-21-wave3-sequencing.md).
Each phase's sessions run under
[`.claude/playbooks/wave-3-session-loop.md`](../../.claude/playbooks/wave-3-session-loop.md)
and self-verify against
[`.claude/playbooks/wave-3-acceptance-criteria.md`](../../.claude/playbooks/wave-3-acceptance-criteria.md).
Each row resolves to the commit reference that closes the phase.

| # | Phase | Status |
|---|---|---|
| W3-P0 | Protocol — sequencing study at [`studies/decisions/2026-05-21-wave3-sequencing.md`](../decisions/2026-05-21-wave3-sequencing.md) → [`docs/adr/0013-wave3-sequencing.md`](../../docs/adr/0013-wave3-sequencing.md) + Wave 3 playbook + Wave 3 acceptance criteria | closed (commit `25e06ab`, 2026-05-21) |
| W3-P1 | ADR promotion — twelve studies (B0-1…B0-7, W2-1…W2-5) plus this sequencing study, into [`docs/adr/0001–0013`](../../docs/adr/) | closed (Session A `c55799c`, Session B `e411fdb`, Session C lands with this commit; all thirteen ADRs exist) |
| W3-P2 | Root infrastructure — `go.work`, `Makefile`, `docker-compose.yml`, `.github/`, `.codex/AGENTS.md`, top-level `README.md`, empty workspace layout | closed (commit lands with this session; depends on B1-10 resolved-study upstream) |
| W3-P3 | Schema-layer — engine schema source, rules schema mirror, `tools/lint` byte-equality gate (B0-1 C2 / C4 / C10) | closed (commit lands with this session; depends on B1-10 resolved-study upstream) |
| W3-P4 | Engine runtime — loader (B0-7), runner (B0-2), result write (B0-3), failure scope (B0-4), orphan detection (B0-7), HTTP trigger handler (ADR-0002 + W3-P4e contract study) | split into W3-P4a / W3-P4b / W3-P4c / W3-P4d (2026-05-21, resolves OQ-W3-2; see [`studies/decisions/2026-05-21-wave3-sequencing.md`](../decisions/2026-05-21-wave3-sequencing.md) §"Phase 4") + W3-P4e (2026-05-22, renumbered from W3-P6b) |
| W3-P4a | Loader — manifest fetch, hash short-circuit, refuse-swap (B0-7 CC1/CC2/CC9; ADR-0005 §4 consumer side) | closed (commit lands with this session; unit + integration tests against the local Compose stack pass) |
| W3-P4b | Result-write layer — append-only `dq_executions` + `dq_check_results` + lazy `dq_executions_current` view (B0-3 CC1/CC2/CC3/CC7) | closed (lands via PR; unit + integration tests against local bigquery-emulator pass; lazy-view fidelity gap honored per ADR-0010 / B1-11 pattern) |
| W3-P4c | Runner + failure-scope mapping — `execution_id` computation (B0-2 CC1/CC2/CC6); status mapping (B0-4 CC1/CC2/CC3/CC4); pre-check validation (B0-7 CC8); observability emission (B0-7 CC14). Depends on W3-P4a and W3-P4b. | closed (lands via PR; engine binary `cmd/dq-engine` builds and runs the periodic loops; unit + integration tests pass; OTel metric+span pipeline deferred — log signal delivered, metric+span land in a follow-up) |
| W3-P4d | Orphan-run detection — periodic scan, follow-up `aborted` row with orphan-detector's `engine_version` (B0-7 CC10/CC11). Depends on W3-P4b. | closed (lands via PR; unit + integration tests pass; partial-failure tolerance + detector-engine-version invariant exercised) |
| W3-P4e | HTTP trigger handler — exposes POST `/v1/trigger`, dispatches to runner per ADR-0002 with the contract committed by ADR-0014. Depends on W3-P4a/b/c/d. Renumbered from W3-P6b on 2026-05-22. | [resolved-study](../decisions/2026-05-22-trigger-handler-contract.md) → [resolved-adr](../../docs/adr/0014-trigger-handler-contract.md) → closed (lands via PR; `engine/internal/api` HTTP handler with strict decoder, async runner dispatch, `/healthz` + `/readyz`; runner enhanced with optional `AttemptID` and `RulesetVersion` overrides for ADR-0007 §3 in-flight isolation; unit + integration tests pass; AC-W3-1..10 verified) |
| W3-P5 | Alerting — Pub/Sub publisher (B0-6), engine-side dedup, `_owners.yaml` schema, linter rule | closed (lands via PR; `engine/internal/alerts` implements `MapCategory` per ADR-0006 CC7, per-attempt `AttemptDeduper` per CC5, JSON Event payload per §4, Pub/Sub v2 publisher; runner + orphan detector wire emissions; engine binary creates the publisher from `DQ_PUBSUB_TOPIC` env; `_owners.v1.schema.json` lands in `rules/_schema/`; `dq-lint` rejects rules whose entity has no `_owners.yaml` entry per CC9; channel encoding committed as `<type>:<id>` per CC2's Wave 3 deferral; unit + integration tests pass) |
| W3-P6 | `rules/` first onboarded entity — end-to-end flow per W2-3 C-W2-3.4 | split into W3-P6a / W3-P6c / W3-P6d (2026-05-21, amended 2026-05-22 — HTTP trigger handler moved to W3-P4e; remaining sub-phases: manifest publisher, first DSL kind interpreter, end-to-end demo, each getting an independent critique pass and PR) |
| W3-P6a | Manifest publisher tool — `tools/manifest/` implements ADR-0005 §4 verify-write-CAS sequence end-to-end | closed (lands via PR; new Go module + `dq-manifest` CLI binary; in-mem Store fake covers CAS race-loser branch; integration test against fake-gcs-server covers happy path + idempotent re-publish + dry-run; CLI exit codes 0/1/2/3/64 documented; B1-11 CAS fidelity gap honored) |
| W3-P6c | First DSL kind interpreter — real `CheckEvaluator` against BigQuery for at least one check kind. Depends on W3-P4c. | closed (lands via PR; `engine/internal/eval` package with `row_count_positive` kind dispatched on `CheckSpec.Kind`; entity identifier defensively validated in the evaluator; engine binary wires the BigQuery-backed evaluator into the runner via new `DQ_SOURCE_PROJECT` / `DQ_SOURCE_DATASET` env vars; unit + integration tests + a runner-eval wire-level integration test pass; runner-overwrites-EvidenceSummary-on-error gap documented in row_count_positive.go and deferred to a follow-up session) |
| W3-P6d | First entity rule YAML + first `_owners.yaml` entry + end-to-end demo target. Depends on W3-P6a, W3-P6c, and W3-P4e. Closes the C-W2-3.4 invariant locally. | closed (lands via PR; first production `rules/customer.yaml` + `rules/_owners.yaml`; new `engine/internal/dsl/spec` strict YAML parser; HTTP handler wires `ResolveChecks` closure that reads the rule YAML body from the object store at trigger acceptance per ADR-0007 §3; `make demo-p6` + `scripts/smoke/demo-p6.sh` exercises manifest publish → loader refresh-short-circuit → execution write → operational-alert capability locally; Go integration test `TestIntegration_DemoP6_EndToEnd` exercises the same flow programmatically; `tools/lint` walker extended to skip underscore-prefixed files so `_owners.yaml` isn't double-validated against the rule schema; unit + integration + demo all green) |
| W3-P7 | `deploy/` — Kubernetes manifests, environment overlays; depends on B1-4 | split into W3-P7a / W3-P7b / W3-P7c (2026-05-22; the first Phase-7 session decided to split; mirrors the W3-P4 split precedent — PAT-4 refactor, deploy base, per-env overlays each get an independent critique pass and PR) |
| W3-P7a | PAT-4 refactor — `engine/internal/env/{local,qa,prod}.go` per foundation 04 §PAT-4 and B1-4 MD-4; engine binary reads `DQ_ENV` selector; reflect-based CI test enforces every-field-in-every-env rule. Depends on B1-4. | closed (lands via PR; new `engine/internal/env` package with `EnvConfig` typed struct + `Local`/`QA`/`Prod` vars; engine binary's `readEnv()` reduces from 13 env vars to 1 selector + 2 emulator overrides; `scripts/smoke/demo-p6.sh` updated to use `DQ_ENV=local`; demo + integration tests + lint all green; `qa.go` / `prod.go` carry `PLACEHOLDER`-marked values to be replaced by the operational session that provisions the real GCP projects) |
| W3-P7b | Deploy base — Kubernetes manifests for the engine in `deploy/base/`. Depends on W3-P7a. | closed (lands via PR; plain Kubernetes YAML — Deployment + Service + ConfigMap + ServiceAccount — tool-neutral so the overlay tool decision stays with W3-P7c per B2-8; probes wired to `/healthz` + `/readyz` per ADR-0014 §4; port 8080 matches `env.Local.HTTPAddr`; image marked `dq-engine:placeholder` for the future release-engineering pipeline (B2-3); base ConfigMap has no data — overlays patch `DQ_ENV` per env plus emulator-host overrides for `local`; pod runs non-root with `readOnlyRootFilesystem`, dropped capabilities, and the `RuntimeDefault` seccomp profile) |
| W3-P7c | Per-env overlays — `deploy/overlays/{local,qa,prod}/` setting `DQ_ENV` and the emulator overrides per env. Depends on W3-P7a + W3-P7b. | closed (lands via PR; Kustomize per B2-8 — `deploy/base/kustomization.yaml` + three overlay directories under `deploy/overlays/{local,qa,prod}/`; each overlay strategic-merges `DQ_ENV` into the `dq-engine-env` ConfigMap; qa/prod overlays additionally annotate the `dq-engine` ServiceAccount with `iam.gke.io/gcp-service-account` per B1-4 MD-3 — values carry `dq-{qa,prod}-PLACEHOLDER` markers mirroring `engine/internal/env/{qa,prod}.go`; local overlay also sets `STORAGE_EMULATOR_HOST` + `BIGQUERY_EMULATOR_HOST` PLACEHOLDERs; new `make validate-deploy` target renders all three overlays via `kubectl kustomize` and CI workflow `.github/workflows/deploy-validate.yml` runs it on every PR; documented deviation from B2-8 CC2's literal `kubectl apply -k --dry-run=client` — that command still performs API-server discovery on a cluster-free runner; deeper schema validation (`kubeconform` or kind-based cluster lane) deferred to B2-3 or a Phase-7 follow-up; Phase 7 closes with this session) |
| W3-P8 | `docs/` content beyond ADRs — glossary, governance, contribution guide, runbook seeds | split into W3-P8a / W3-P8b / W3-P8c / W3-P8d (2026-05-22; mirrors the W3-P4 / W3-P6 / W3-P7 split precedent — each sub-phase gets an independent critique pass and PR) |
| W3-P8a | Glossary — `docs/glossary.md` collecting terms with codebase-specific meaning across the ADRs, foundation documents, and resolved decisions. Forward-only per R8; no back-links into `studies/` from new content. | closed (lands via PR; ~50 terms across eleven thematic groups — governance, schema boundary, run identity, storage, failure model, manifest plane, loader/runner, trigger plane, alerting, DSL, environment; each entry cites its defining ADR / foundation §) |
| W3-P8b | Governance — finalize CODEOWNERS for the asymmetric review model from ADR-0001 plus contribution-time roles; the study→ADR promotion flow already lives in `.claude/commands/promote-to-adr.md`. Depends on B1-9. | closed (lands via PR; `/.github/CODEOWNERS` published with `PLACEHOLDER-org/` literals per the path-rule table committed by B1-9; `docs/governance.md` lands as a forward-only referential summary of the review model and contribution-time flows; `rules/_owners.yaml`'s `customer` entity updated to `owner: "@PLACEHOLDER-org/rules-authors"` per B1-9 Consequence #4; substitution to the real org slug remains deferred to the operational session that creates the production org per ADR-0008 follow-up + OQ-B1-9.1) |
| W3-P8c | Contribution guide — how to add a rule, how to run `make demo-p6`, how to open a B-item, how a Wave 3 session loop closes. Depends on W3-P8a (terminology baseline). | closed (lands via PR; the stale Wave-1-only `/CONTRIBUTING.md` rewritten in place as the authoritative guide per GitHub convention; covers four practical flows — add a rule, run `make demo-p6`, open a B-item, close a Wave 3 session loop — plus a "what review will look like" pointer to `docs/governance.md`; `docs/README.md` updated to point at the root file; commit conventions section refreshed to mirror the post-Wave-1 `feat(...)`/`docs(...)` taxonomy) |
| W3-P8d | Runbook seeds — operator-facing playbooks for manifest rollback via CAS pointer write, orphan-run remediation, alert-dedup debugging, refresh-failure escalation. Depends on W3-P8a (terminology baseline). | closed (lands via PR; new `docs/runbooks/` directory with index + four runbooks, each following the fixed shape `when to use → preconditions → procedure → verification → rollback → escalation`; each runbook flags TBD markers where a B1 numeric parameter is unresolved (e.g., B1-2 refresh-failure thresholds) or where a CLI subcommand the procedure would prefer does not exist yet (e.g., `dq-manifest set-pointer`, blanket orphan-finalization tool); `docs/README.md` updated to advertise the runbooks directory; **Phase 8 closes with this session — all sub-phases W3-P8a / W3-P8b / W3-P8c / W3-P8d are now closed**) |

---

## Wave-S — Record-Oriented Capability Decisions

Wave-S launches record-oriented (stream-based) validation capability
parallel to the set-oriented capability delivered through Waves 1-3.
Launch study at
[`studies/decisions/2026-05-23-wave-s-launch.md`](../decisions/2026-05-23-wave-s-launch.md)
→ [ADR-0020](../../docs/adr/0020-wave-s-launch.md). Each B0-S item below opens
its own study under the `/resolve-b0` protocol that landed B0-1
through B0-7; sequencing and gate criteria are committed in the
launch study §6.3.

| # | Topic | Status | Key Question | Why It Matters | Expected Output |
|---|---|---|---|---|---|
| B0-S1 | Mode as primitive | [resolved-study](../decisions/2026-05-23-b0-s1-mode-as-primitive.md) → [resolved-adr](../../docs/adr/0021-mode-as-primitive.md) | How is `mode` declared on the rule artefact and entity, and how does capability derive from it (per P3)? | Mode is the architectural primitive (P1); without explicit declaration, downstream capability drifts and the kind-prefix lint gate cannot enforce the boundary. | ADR + mode-field schema + lint rule under `tools/lint/` |
| B0-S2 | Kind catalog | [resolved-study](../decisions/2026-05-24-b0-s2-kind-catalog.md) → [resolved-adr](../../docs/adr/0022-kind-catalog.md) | What is the registry of supported `set.*` and `record.*` kinds, and how is the catalog extended? | Without a catalog, source declarations (S3) cannot validate against a kind's expected shape; schema-version governance has nothing to bump. | ADR + `record.*` schema half under `engine/schema/` and `rules/_schema/` |
| B0-S3 | Sources schema | [resolved-study](../decisions/2026-05-24-b0-s3-sources-schema.md) → [resolved-adr](../../docs/adr/0023-sources-schema.md) | How is a source described per mode — set source (BigQuery table/view) vs record source (stream substrate topic/subscription)? | Sources cross-check against the kind catalog and extend the ADR-0007 loader; last item of the foundational triplet — its promotion meets the partial-Wave-S gate. | ADR + source schema + loader extension |
| B0-S4 | Window semantics | [resolved-study](../decisions/2026-05-24-b0-s4-window-semantics.md) → [resolved-adr](../../docs/adr/0024-window-semantics.md) | What does "window" mean for record-mode (tumbling, sliding, session, watermark-bounded, or per-record), and how do watermarks interact with the ADR-0002 `execution_id` formula? | Windowing reshapes the record-mode halves of ADR-0002 (identity), ADR-0003 (write model), and ADR-0006 (dedup). | ADR + record-mode `execution_id` rule |
| B0-S5 | Aggregation & unified-vs-parallel execution | [resolved-study](../decisions/2026-05-24-b0-s5-aggregation-and-runner-shape.md) → [resolved-adr](../../docs/adr/0025-aggregation-and-runner-shape.md) | One unified runner switching on mode, or two parallel runners — against what objective criterion (satisfies P4's deferral)? | Decides the engine binary layout under `engine/cmd/`; reshapes ADR-0007 (loader/scheduler) and ADR-0014 (trigger handler) on the record side. | ADR + objective criterion + runner-shape decision |
| B0-S6 | Failure scope aggregated | [resolved-study](../decisions/2026-05-24-b0-s6-failure-scope-aggregated.md) → [resolved-adr](../../docs/adr/0026-failure-scope-aggregated.md) | How do per-record failures aggregate into an entity-level status (per ADR-0004) when record-mode lacks a natural batch boundary? | Reshapes the record-mode half of ADR-0004 (status policy) and ADR-0006 (alert routing); B1-6 (evidence retention) needs a record-mode amendment. | ADR + record-mode status mapping + B1-6 amendment scope |
| B0-S7 | Record-oriented cost guardrails | [resolved-study](../decisions/2026-05-24-b0-s7-record-mode-cost-guardrails.md) → [resolved-adr](../../docs/adr/0027-record-mode-cost-guardrails.md) | What throughput, backpressure, dead-letter, and consumer-lag ceilings apply to record-mode under each environment (per ADR-0018)? | Cost couples to substrate (DD-S.6); without ceilings, record-mode lacks the cost discipline (P4) that set-mode gets from B1-2. | ADR + per-env ceilings + ADR-0019 overlay extension under `deploy/overlays/` |

---

## Process

### When a decision moves from `open` to `in-progress`

A draft document exists in `studies/decisions/` (typically created by
the `/resolve-b0` command for B0 items, or by ad-hoc study for B1/B2).
The row is updated with a link to the draft.

### When a decision moves to `resolved-study`

The study is complete: its Open Questions section is either empty or
contains only items explicitly accepted as out-of-scope for the
current cycle. The row is updated with the final study path.

### When a decision moves to `resolved-adr`

The study has been rewritten as an ADR under `docs/adr/` during Wave
3. The row keeps the original study link **and** adds the ADR link.
The study stays in `studies/decisions/` as historical reasoning; it
is not deleted.

### When a new decision is discovered

If a working session reveals a decision that is not yet in the log,
the session adds it before resolving it. Decisions found mid-session
should be tracked here even if not immediately worked on. This keeps
the log honest.

---

## Wave Gates

Use this section to confirm whether the project can advance.

### Wave 1 gate (B0 complete)

Pass when **every B0 row** is at status `resolved-study` or
`resolved-adr`. Currently: **7 of 7 resolved-adr — gate met**
(all B0 studies promoted to `docs/adr/0001-0007` on 2026-05-21).

### Wave 2 gate (platform decisions complete)

Pass when the consolidated Wave 2 decisions document exists in
`studies/decisions/` and addresses every W2 row. Currently:
**5 of 5 W2 rows resolved-adr — gate met**
(study at
[`studies/decisions/2026-05-21-platform-decisions-wave2.md`](../decisions/2026-05-21-platform-decisions-wave2.md);
ADRs at `docs/adr/0008–0012`, promoted 2026-05-21).

### Wave 3 readiness

Pass when both Wave 1 gate and Wave 2 gate have passed. Wave 3 (full
scaffolding) cannot start before this. Currently: **gate met as of
2026-05-21**; phase progression tracked in the
[Wave 3 — Phases](#wave-3--phases-scaffolding-sequencing) table
above.

### Wave 3 completion gate (all phases scaffolded)

Pass when **every row in the [Wave 3 — Phases](#wave-3--phases-scaffolding-sequencing)
table is closed**. Currently: **9 of 9 phases closed — gate met as
of 2026-05-23** (W3-P0 through W3-P8 all closed; the closing
sub-phases are W3-P4a/b/c/d/e, W3-P5, W3-P6a/c/d, W3-P7a/b/c, and
W3-P8a/b/c/d).

Five resolved-study items inside or adjacent to Wave 3 are now
promoted to ADRs alongside this gate: **B1-9 → ADR-0015** (CODEOWNERS),
**B1-10 → ADR-0016** (workspace tooling), **B1-11 → ADR-0017**
(substrate-posture amendment to ADR-0010), **B1-4 → ADR-0018**
(environment configuration model), **B2-8 → ADR-0019** (infrastructure
tooling — Kustomize). The remaining open B1 and B2 rows above are
demand-driven follow-ups, not Wave 3 blockers.

**Post-Wave-3 follow-up backlog** (work that survives Wave 3 closure):

- **Open B1 rows — none.** All seven B1 rows are now
  `resolved-adr` as of 2026-05-25:
  - B1-1 (baseline strategy) → ADR-0032.
  - B1-2 (BigQuery cost ceilings) → ADR-0029.
  - B1-3 (scheduler catchup behavior) → ADR-0033.
  - B1-4 (environment configuration model) → ADR-0018
    (resolved earlier; B1-4 closed pre-Wave-S).
  - B1-5 (local testing strategy) → ADR-0034.
  - B1-6 (evidence retention parameters) → ADR-0031.
  - B1-7 (compatibility window duration) → ADR-0035.
  - B1-8 (manifest cryptographic posture) → ADR-0030
    (B0-5 reopener did not fire).
  - B1-9 (CODEOWNERS finalization) → ADR-0015 (Wave-3).
  - B1-10 (workspace tooling stack) → ADR-0016 (Wave-3).
  - B1-11 (substrate-posture amendment) → ADR-0017
    (Wave-3).
  Seven new B1-tier ADRs landed on 2026-05-25
  (0029–0035); five carried deferred-implementation
  posture (ADR-0030 / ADR-0032 / ADR-0033 / ADR-0034 /
  ADR-0035) with B2 follow-ups for the consumer slices.
- **Open B2 rows** — twenty-two rows total at 2026-05-25
  numbering:
  - **Long-tail B2 rows from earlier waves** (B2-1, B2-2,
    B2-3, B2-4, B2-5, B2-6, B2-7, B2-9, B2-10): seven
    original implementation-phase items + the W3-P8 follow-ups
    (B2-9 owner ↔ CODEOWNERS-group linter cross-check; B2-10
    `dq-manifest set-pointer` rollback subcommand).
  - **B1-closure follow-ups numbered 2026-05-25** (B2-11
    through B2-23): thirteen rows registered when the seven
    B1-tier ADRs (0029–0035) landed. Source-ADR mapping:
    B2-11/B2-12 from ADR-0029; B2-13 from ADR-0031;
    B2-14/B2-15 from ADR-0032; B2-16/B2-17 from ADR-0033;
    B2-18 from ADR-0034; B2-19/B2-20/B2-21/B2-22/B2-23 from
    ADR-0035.
  Total open B2 rows: 22 (B2-8 resolved-adr → ADR-0019).
  Triage focus shifts to B2; suggested ordering documented
  in [§Recommended Next Sequence](#recommended-next-sequence)
  below.
- **Operational `PLACEHOLDER` substitutions** awaiting the
  GitHub-org / GCP-project provisioning session: `@PLACEHOLDER-org/…`
  in `/.github/CODEOWNERS` and `rules/_owners.yaml`;
  `dq-{qa,prod}-PLACEHOLDER-*` identifiers in
  `engine/internal/env/{qa,prod}.go` and the matching deploy overlays.
- **Runbook TBDs** under `docs/runbooks/` now resolved by ADR-0029
  for refresh-failure parameters; remaining TBDs wait on B2-10's
  CLI surface (`dq-manifest set-pointer` for the rollback runbook).

These follow-ups do not block any wave gate; each is picked up
on demand under the same study → critique → promotion protocol that
governed Waves 1–3.

---

## Recommended Next Sequence

Waves 1, 2, and 3 are complete (gates met 2026-05-21, 2026-05-21,
and 2026-05-23 respectively). The original 9-step sequence below is
historical; the live sequence from 2026-05-23 onward is demand-
driven against the post-Wave-3 follow-up backlog enumerated in the
Wave 3 completion gate section above.

Suggested triage order when starting a follow-up session:

1. **Operational unblocks first.** If a `PLACEHOLDER` substitution
   or a missing CLI surface (e.g., B2-10) is blocking an imminent
   operational task, resolve it before any B1 study session.
2. **All B1 rows are resolved.** B1-1…B1-11 are all at
   `resolved-adr` as of 2026-05-25. The remaining
   work flowing from B1 closure is registered as B2
   follow-ups (consumer slices that ship the
   implementations the design-only ADRs deferred); no
   open B1 rows remain to prioritize. Triage focus
   shifts to the B2 list + operational `PLACEHOLDER`
   substitutions.
3. **B2 rows last**, on demand. B2-9 (linter cross-check) and B2-10
   (CLI rollback subcommand) are demand-driven; the other B2 rows
   surface as implementation reveals concrete needs.

### Historical sequence (Waves 1–3)

1. Resolve B0-1 (compatibility model) — it underpins B0-5 and B0-7.
2. Resolve B0-5 (manifest semantics) — required before loader
   semantics can be specified.
3. Resolve B0-2 (run identity) — required before result write model.
4. Resolve B0-3 (result write model) — depends on B0-2.
5. Resolve B0-4 (failure scope) — depends on B0-2 and B0-3.
6. Resolve B0-7 (loader and scheduler failures) — depends on B0-1,
   B0-5, B0-4.
7. Resolve B0-6 (alert routing) — depends on B0-4.
8. Run Wave 2 consolidated decision session.
9. Begin Wave 3 scaffolding.

This ordering minimized rework: each decision built on stable ground
from the previous.
